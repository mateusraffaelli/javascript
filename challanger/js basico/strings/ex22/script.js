//Write code to transform the string str so that its first and last letters are uppercase, and the middle letter is lowercase.Use string indexing (e.g., str[0]), toUpperCase(), and toLowerCase() in your solution.

let str = 'aBc';
 
// add code here...
 
console.log(str); // expected: "AbC"
 
//let str = 'aBc';
//str = str[0].toUpperCase() + str[1].toLowerCase() + str[2].toUpperCase();
//console.log(str);