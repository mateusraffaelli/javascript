function makePerson(name, age) {
  // complete the function below
  
}

const person = makePerson('Bob', 25);
console.log(person.name === 'Bob' && person.age === 25);








/*
function makePerson(name, age) {
  // long syntax: key and value are both written
  return { name: name, age: age };
}


// shorthand: if key and value have the same name, we can write it shorter

function makePerson(name, age) {
  return { name, age };
}
*/