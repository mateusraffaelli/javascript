//In this exercise, the function func defines the parameters a and b. But, currently only 1 argument is passed to the function. Therefore, the value of b is undefined. Adjust the code below so that the console.log() statement logs hello world.

function func(a, b) {
    return b + a;
 };
  
 const result = func('hello');
  
 console.log(result);


//const result = func(' world', 'hello');  