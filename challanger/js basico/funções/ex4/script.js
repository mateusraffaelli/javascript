//The following code is intended to define a function named greet which takes a name and returns a greeting. However, the function keyword is missing. Fix the definition so that it works as intended.

// add missing function keyword
greet(name) {
    return 'Hello, ' + name + '!';
}
    
const message = greet('Alice');
console.log(message);

//function greet(name) {