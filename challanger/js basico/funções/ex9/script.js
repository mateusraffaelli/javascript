//In the code below, we define a function multiply that has a parameter called num. The function returns num multiplied by itself. Adjust the code below such that the console.log() statement logs true.

function multiply(num) {
    return num * num;
 };
  
 // adjust code below...
 const arg = 3;
  
 const result = multiply(arg);
 console.log(result === 25);
  

 //const arg = 5;