const mixed = [10,4,'c',6,'a',4];
const newArray = [];


for () {
   const current = mixed[i];
   if (typeof current === 'number') {
       newArray.push(current);
   }
}


console.log(newArray);
//expect output: [10,4,6,4]







/*
const mixed = [10,4,'c',6,'a',4];
const newArray = [];


for (let i = 0; i < mixed.length; i++) {
   const current = mixed[i];
   if (typeof current === 'number') {
       newArray.push(current);
   }
}


console.log(newArray);
*/