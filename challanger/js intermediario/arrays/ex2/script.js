const colors = ['red', 'green', 'blue'];
const result = [];

// Use for ... off loop to iterate over all elements in the colors array and push each color to the result array.

console.log(result);







/*
const colors = ['red', 'green', 'blue'];
const result = [];
for (const color of colors) {
  result.push(color);
}
console.log(result);
*/